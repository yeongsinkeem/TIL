class Solution {
    public String solution(String my_string, String alp) {
        if ( my_string.contains(alp) ) {
            String str = alp.toUpperCase();
            my_string = my_string.replace(alp, str);
        }
        
        return my_string;
    }
}
