class Solution {
    public String solution(String myString) {
        // 1. 모든 문자열 소문자로 변환
        myString = myString.toLowerCase();
        
        // 2. a -> A 변환
        myString = myString.replaceAll("a", "A");
        return myString;
    }
}
