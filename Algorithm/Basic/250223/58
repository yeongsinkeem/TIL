class Solution {
    public boolean solution(boolean x1, boolean x2, boolean x3, boolean x4) {
        boolean answer = true;
        
        boolean m = x1 || x2;
        boolean n = x3 || x4;
        
        answer = m && n;
        return answer;
    }
}
